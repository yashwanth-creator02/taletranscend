/**
 * src/pages/shelf/layout.js
 * Handles the visual layout initialization for the shelf page.
 */

import { initIcons } from '@ui/components/icons.js';

export function initShelfLayout() {
  // Currently, the shelf page layout is primarily handled by CSS.
  // We initialize icons and any other visual-only enhancements here.
  initIcons();
}
