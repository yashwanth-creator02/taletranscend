// src/pages/contribution/publish.js
// Placeholder for the tale publishing workflow.
// Publishing logic will be implemented in a future iteration.

import { state } from './state.js';

/**
 * Initiates the tale publishing flow.
 * Currently logs chapter data and shows a placeholder alert.
 * This will be replaced with a full publish pipeline.
 */
export function publishFullTale() {
  console.log('Publishing tale:', state.chapters);
  alert('Publish flow coming next');
}
